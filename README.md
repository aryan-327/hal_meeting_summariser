# hal_meeting_summariser

prompt



->
currently the project has the option to upload audio file or text file of the conversation and then generate summary.
update the code so that instead of uploading the audio file there should be button to start recording and then stop recording , a timer should also be visible while recording , then after recording is stopped, it should generate accurate summary that covers all imp points for eg deadlines ,task assigned , problems , solns , and then there shoukd be an option to download the dsummary in formof pdf , and if mail id are provided it can also be used to share the summary pdf via mail to recipients , the project should use device microphone for recording , and raise error before hand only if it is unable to record from the mic , so that after meeting a proper result is gaurenteed.

currently there are only .py files , which is fine for functionlality but not impressive look. 

->Act as an elite frontend UI/UX engineer. Build the structural layout component for a high-density, aero-military "Command Center" dashboard designed for a meeting summarizer. 
create professional ui front end file , that should not look like ai generated , but by an professional front end designer

Use React and Tailwind CSS. The design system must feel ultra-premium and professional, impressive,optimized for data density without visual clutter.

Apply these exact specifications:
1. Core Grid System: Implement a 3-column Bento Grid structure. 
2. Glassmorphism Aesthetics: All cards must use a translucent layer look. Apply `bg-slate-950/40`, `backdrop-blur-xl`, a microscopic border of `border-[0.5px] border-white/10`, and a deep structural drop shadow (`shadow-[0_8px_32px_0_rgba(0,0,0,0.4)]`).
3. Precision Typography: Set the global background to a solid deep dark slate (`bg-slate-950`). Use sharp, high-readability sans-serif fonts for content (like Inter or Geist) and clean monospace utilities (like JetBrains Mono) for metadata tags, timestamps, and classifications.
4. Precision Details: Use wireframe telemetry-style icons (via Lucide React or similar) instead of rounded or playful icons. Ensure all card heights are uniform and scroll independently when filled with content.

Provide only the clean, modular React component structure with placeholder content inside the main panels.


-> add feature that there should be option to view previous meeting summaries with the meeting date , time , duration , a generated meeting title.
